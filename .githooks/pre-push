#!/bin/sh
# Runs before git push. Blocks push when checks fail.
set -e

root="$(git rev-parse --show-toplevel)"
cd "$root"

echo "pre-push: running format-check, Go/frontend lint, and vuln-check..."
make pre-push
